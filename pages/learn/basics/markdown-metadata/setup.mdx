import Layout from '../../../../components/learn/Layout';
import Link from 'next/link';

export const meta = {
  title: 'Markdown and Metadata',
  courseId: 'basics',
  lessonId: 'markdown-metadata',
  stepId: 'setup'
};

## Setup (Optional)

**If you’re continuing from the previous lesson,** you can skip the following and go straight to the next page.

Otherwise, you can download, install, and run the starter code below. Again, this is NOT necessary if you’ve just finished the previous lesson.

```bash
git clone https://github.com/zeit/next-learn-demo.git
cd next-learn-demo
cd 3-markdown-and-metadata
npm install
npm run dev
```

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
