import Layout from '../../../../components/learn/Layout';
import Link from 'next/link';

export const meta = {
  title: 'Markdown and Metadata',
  courseId: 'basics',
  lessonId: 'markdown-metadata'
};

We’re building a blog, but we haven’t added any content. Let’s add it using Markdown. Next.js supports [MDX](https://mdxjs.com/), which lets you write JSX in Markdown documents, via [an official plugin](https://github.com/zeit/next.js/tree/canary/packages/next-mdx).

In this lesson, you’ll also learn how to add metadata like `title` tags using `next/head`.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
