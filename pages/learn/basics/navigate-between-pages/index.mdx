import Layout from '../../../../components/learn/Layout';
import Link from 'next/link';

export const meta = {
  title: 'Navigate Between Pages',
  courseId: 'basics',
  lessonId: 'navigate-between-pages'
};

In this lesson, you’ll learn how **page navigation** works in Next.js.

> If you’re looking for detailed documentation instead of a tutorial, take a look <Link href="https://nextjs.org/docs/routing/introduction"><a>here</a></Link>.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
