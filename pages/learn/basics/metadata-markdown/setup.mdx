import Layout from '../../../../components/learn/Layout';
import Link from 'next/link';

export const meta = {
  title: 'Metadata and Markdown',
  courseId: 'basics',
  lessonId: 'metadata-markdown'
};

TBD

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
