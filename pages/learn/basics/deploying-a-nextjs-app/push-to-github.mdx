import Layout from '../../../../components/learn/Layout';
import AnswerBox from '../../../../components/learn/AnswerBox';

export const meta = {
  title: 'Deploying a Next.js App',
  courseId: 'basics',
  lessonId: 'deploying-a-nextjs-app',
  stepId: 'push-to-github'
};

## Push to GitHub

First, choose any Next.js app you’ve developed so far and **push it to GitHub**.

1. Create [a new repository on GitHub](https://github.com/new). It can be public or private. You do NOT need to initialize with a README.
2. Follow the directions on GitHub and push your Next.js app.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
