import Layout from '../../../../components/learn/Layout';

export const meta = {
  title: 'Getting Started',
  courseId: 'basics',
  lessonId: 'getting-started',
  stepId: 'errors'
};

## Error Messages in the Browser

It should have automatically shown you an error message in the browser. In Next.js, most errors you’ll encounter will be displayed in the browser. You usually won’t need to look at your terminal to debug.

If you uncomment the last line and save the file, the browser will show the page automatically again.

```jsx
const Index = () => <h1>Test Test</h1>;

export default Index;
```

That’s it for the first lesson. Good job!

In the next lesson, we’ll start adding more pages to our app.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
