```jsx
export default function TitleBlock({ title, description }) {
  return (
    <div>
      <h1>{ title }</h1>
      <p>{ description }</p>

      <style jsx>{\`
        h1 { font-size: 32px; margin-bottom: 16px; }
        p { font-size: 16px; }
      \`}</style>
    </div>
  )
}
```
